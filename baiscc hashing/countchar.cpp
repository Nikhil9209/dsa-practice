#include<bits/stdc++.h>
using namespace std;
int main(){
    string name;
    cin>>name;
    unordered_map<string,int> mpp;
    for(int i =0;i<name.length();i++){
        mpp(name[i])++;
    }
        
}
